#!/usr/bin/env bash
# 00-build-reference.sh — derive script hashes from contracts/config/network.<net>.json,
# parameterize one_shot_mint, mix_box, fee_contract, write artifacts/<net>/{*.plutus, addresses.json}.
#
# M0 placeholder. Real implementation lands in M2 alongside contracts/build.sh.
# See docs/spec/03-contracts.md §4 and docs/spec/12-build-guide.md §"Within M2".
set -euo pipefail
echo "infra/bootstrap/00-build-reference.sh: not yet implemented (lands in M2)" >&2
exit 1
